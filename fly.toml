app = "tailscale-silow"
primary_region = "sin"
kill_signal = "SIGINT"
kill_timeout = "5s"

[build]

[mounts]
  source = "tailscale_state"
  destination = "/var/lib/tailscale"
  initial_size = "1gb"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
